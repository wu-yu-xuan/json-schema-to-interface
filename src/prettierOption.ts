import { Options } from 'prettier';

const prettierOptions: Options = {
  semi: true,
  singleQuote: true,
  printWidth: 80,
  tabWidth: 2,
  trailingComma: 'none',
  useTabs: false,
  bracketSpacing: true
};

export default prettierOptions;
